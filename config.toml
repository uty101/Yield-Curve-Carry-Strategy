# curvecarry configuration.
#
# Every number that affects a result lives in this file and nowhere else
# (global rule 4). It is written in full in step 0.1; a later step may change
# a default only if PLAN.md says so, and that change is a commit on its own
# (global rule 14). Keys left as "" are decisions pending from session 1.
#
# Units: yields and rates are decimals (0.0425 = 4.25%). Tenors are years.
# Durations are years. Costs are basis points. Windows are months.

# ---------------------------------------------------------------- universe
base_currency = "USD"                 # issue #1 answer 1; FX is stored as units of base per unit of foreign
countries = ["US", "GB", "DE", "JP", "CA", "FR"]   # IT dropped, session-1 correction 3: decisions/euro_curves.md
tenors = [1, 2, 3, 5, 7, 10, 20, 30]  # standard tenor grid, years
short_end = "flat"                    # below the shortest observed zero tenor the zero yield is flat from that tenor; this is a
                                      # convention, not extrapolation, and is the only exception to rule 13, used only by
                                      # bondmath and bootstrap (plan v2 amendment B; decisions/short_anchor.md)
bootstrap_max_node_gap_years = 10     # Session 1 part B fix 2: the par bootstrap stops at the last observed tenor before two
                                      # consecutive observed nodes are further apart than this; tenors beyond are absent
                                      # (US 20y/30y 1987-01..1993-09, DGS20 unpublished; decisions/sources.md)
bootstrap_zero_par_tolerance_bp = 100 # Session 1 part B fix round 2 (replaces bootstrap_forward_tolerance_bp, withdrawn): the first
                                      # standard tenor >= 10y that is an observed par node and whose bootstrapped zero is more
                                      # than this from the par yield there, and every tenor beyond, is dropped for that month.
                                      # Chosen after seeing the data; the band is in decisions/sources.md

# coupon frequency per country, payments per year; used by the bootstrap (1.9)
# and by every par-bond price/duration/convexity function (2.1)
[coupon_frequency]
US = 2
GB = 2
JP = 2
CA = 2
DE = 1
FR = 1
# IT = 1                             # kept for reference; Italy is not in `countries`

# currency of each country; the funding rate, hedge and FX loaders key on it
[currency]
US = "USD"
GB = "GBP"
DE = "EUR"
JP = "JPY"
CA = "CAD"
FR = "EUR"

[sample]
strategy_start = "1997-08-31"                   # set in step 1.9 from data/checks/sample_window.txt (YYYY-MM-DD)
sample_full_start = "2004-11-30"                # set in step 1.9: first month all 6 countries are present (France is the binding one)
strategy_end = "2026-08-31"           # last complete month at the time of writing (session 1, 2026-09-16)
sample_min_countries = 5              # strategy_start = first month where >= this many of the 6 countries ...
sample_max_tenor = 10                 # ... have every tenor up to and including this one

# ------------------------------------------------------------ curve models
[nelson_siegel]
ns_lambda_grid = [0.05, 1.5, 0.05]    # lambda grid: lo, hi, step (per year). Lower bound widened from 0.2 in
                                      # session 2 (issue #12 answer Q1, option C): the Bundesbank's own lambda was
                                      # outside [0.2, 1.5] in 69.9% of DE months and our own NS-free lambda sat on a
                                      # bound in a third of all months, ~90% of those at the lower bound. The upper
                                      # bound stays at 1.5: a published lambda above it is a month where tau
                                      # collapses, not a curve we need to reach. 30 NS grid points, 435 Svensson pairs.
ns_lambda_fixed = 0.7                 # Diebold-Li fixed lambda
min_tenors = 5                        # a country-month with fewer observed standard tenors is not fitted (2.2, 2.3)

[pca]
pca_min_months = 60                   # minimum months before an expanding-window PCA produces a score
pca_z_window = 36                     # rolling window for the PC2 z-score (6.7)
stability_min_months = 24             # a decade with fewer months of changes is not reported in 3.2
tenor_presence_min = 0.95             # 3.1, session-3 amendment 1 (issue #15): a standard tenor is in a country's PCA
                                      # tenor set if it is present in at least this share of that country's months from
                                      # the first month the country has a 10-year zero (option C; PLAN.md 3.1)
us_borderline_gap_bp = 50             # 3.2, session-3 amendment 4 (issue #15): the US decade rows are written a second
                                      # time excluding the pre-1997 months whose 30-year zero-to-par gap in
                                      # data/checks/par_zero_gap.csv exceeds this. A robustness check: 30y is not in the
                                      # US PCA set under option C, so those months reach no loading through 30y

# ----------------------------------------------------------------- strategy
[signal]
yield_floor = 0.002                   # buckets with zero yield below 20 bp are excluded that month (brief 10)

[weights]
long_duration_budget = 5.0            # years of duration per unit capital in the long leg (brief 6.5)
duration_neutral_scope = "book"       # issue #1 answer 3; "country" is a logged robustness run in 5.5
min_eligible_buckets = 9              # a month with fewer eligible buckets holds nothing (5.2); logged.
                                      # Raised from 6 by session-5 amendment 3, pre-registered before any result was
                                      # seen: below 9 eligible buckets k = n // 3 is 2 a side, a pair trade and not a
                                      # book. Global rule 14: this change is a commit on its own

[overlay]
z_entry = 1.5                         # |z| beyond which a steepener/flattener is entered
z_exit = 0.0                          # z level that closes a position (cross back through)
overlay_duration_budget = 2.0         # years of duration in the long leg of the 2s10s pair
overlay_tenors = [2, 10]              # the pair, years

[hedge]
funding_rate = "policy"               # default funding/hedge rate: BIS WS_CBPOL policy rate per currency (session-1 correction 2)
funding_rate_robustness = "interbank_3m"  # robustness column: OECD 3-month interbank, FRED IR3TIB01{US,GB,JP,CA,EZ}M156N
eur_splice = "1999-01-31"             # EUR funding: the national BIS policy rate (M.DE, M.FR) before this month end, M.XM from it (amendment D)

[hedge.eur_legacy]                    # BIS WS_CBPOL area code of each euro country before eur_splice
DE = "DE"
FR = "FR"

[costs]
cost_bp_per_duration_year = 0.5       # bp per year of duration traded, applied to sum |delta(w*D)|

# ------------------------------------------------------------------ checks
[checks]
identity_gap_flag_bp_per_year = 50    # 4.2, session-4 amendment 2: data/checks/return_identity.csv flags a
                                      # country-tenor whose annualised mean r_local differs from its annualised
                                      # mean (yield/12 + rolldown) by more than this, in bp a year. A reporting
                                      # threshold on a check, not a gate: nothing stops on it

# -------------------------------------------------------- risk controls (6.2)
[risk]
target_vol = 0.05                     # annualised vol target for the vol-scaled variant
vol_window_months = 12                # trailing window for realised vol
max_leverage = 2.0                    # cap on the vol-target scalar
dd_stop = 0.10                        # drawdown from peak that flattens the book
dd_reentry_months = 3                 # months flat before re-entry after a stop
rates_vol_pct = 0.90                  # expanding percentile of pooled PC1 12m vol above which the book is flat
risk_control_base_variants = ["carry_hedged", "combined_hedged"]  # the books each control in 6.2 is applied to

# ------------------------------------------------------------------ report
[report]
chart1_decades = [1990, 2000, 2010, 2020]  # one fitted-vs-observed date per decade per country (2.4)
