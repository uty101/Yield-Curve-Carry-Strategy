# Review — Step 5.3 — The carry-only backtest, and the headline

Date: 2026-09-23   Commit: 605b907   Tests: 260/260

## What changed

- `src/curvecarry/metrics.py` (new) — `annualised_return`, `annualised_vol`,
  `sharpe`, `max_drawdown` (with peak and trough dates), `turnover_mean`,
  `summary`.
- `src/curvecarry/backtest.py` (new) — `Variant`, `VARIANTS`, `run`,
  `run_all`, `available_signal`, `bucket_returns`, `monthly_returns`,
  `metric_rows`, `extreme_months`, `merge_overrides`, `variant_note`.
- `src/curvecarry/cli.py` — `run --variant <name> | --all [--note]`.
- `tests/test_backtest.py` (new) — 10 tests, all synthetic, all with the
  spec-log path injected into `tmp_path` so no test ever appends a row to
  the real `reports/specifications.csv`.
- `PLAN.md` — amended again in-session: `extreme_months_<variant>.csv` is
  written by the 5.3 engine for every variant rather than once in 5.5, and
  every metric is reported gross **and** net.
- New outputs: `reports/specifications.csv` (2 rows),
  `data/processed/backtest_carry_{hedged,unhedged}.parquet`,
  `data/checks/metrics_carry_{hedged,unhedged}.csv`,
  `data/checks/positions_carry_{hedged,unhedged}.parquet`,
  `data/checks/extreme_months_carry_{hedged,unhedged}.csv`.

**The headline, and it was fixed before these numbers were seen**
(amendment 1, written into `PLAN.md` 5.3 and `CLAUDE.md` in commit
`c77be42`, which precedes every commit that computes anything):
`carry_hedged`, `full` window, `r_net`.

**Three decisions this step made that the plan left open**, all visible in
the module docstring:

1. **Availability is applied before sizing, not after.** The plan says a
   bucket with a NaN return "is not held". Dropping it after the legs were
   sized would leave `Σ wd ≠ 0`. It is instead marked ineligible in the
   signal frame, and the weights are built on what remains, so neutrality
   survives.
2. **A flat month still gets a row.** A month the 5.2 minimum flattens earns
   0 and is charged the cost of coming out of last month's book. Silently
   omitting it would shorten the sample and hide the exit cost.
3. **`exclude_buckets` (for the 5.5 GB-30y variant) is part of the variant
   definition in `VARIANTS`, not a config knob.** Adding a knob to
   `config.toml` that PLAN.md never named would breach rule 14; the
   exclusion is instead recorded in the spec-log `note` as JSON, like an
   override.

**One bug found and fixed while writing the tests.** `max_drawdown` first
took the running peak as `wealth.cummax()`, which starts at the first
month's wealth — so a series that falls in its first month showed a
drawdown of 0. The running peak now starts at **1.0, the level at
inception**, which is what the plan's own closed-form test asks for
(`−10%` then `+5%` → `−0.10`). A peak at inception has no date in the
series and `dd_peak` is then an empty cell.

## Findings

> Claim: the headline. Carry-only, hedged, book-scope, net of costs, 1997-08-31 to 2026-07-31.
> Number: annualised return **0.591%**, annualised vol **2.233%**, Sharpe **0.265**, max drawdown **−10.37%** from 2019-12-31 to 2022-12-31, mean turnover **1.473** years of duration a month, 2022 in isolation **−4.41%**. Gross of costs: 0.767% and Sharpe 0.344. 348 months.
> Rows (variant, window, basis, ann_return, ann_vol, sharpe, max_dd, peak, trough, turnover, 2022, n):
> carry_hedged   full  net   0.5907% 2.2329% 0.265 −10.3653% 2019-12-31 2022-12-31 1.473 −4.4131% 348
> carry_hedged   full  gross 0.7675% 2.2329% 0.344 — — — — 348
> carry_hedged   six   net   0.4792% 2.0053% 0.239 −10.3653% 2019-12-31 2022-12-31 1.483 −4.4131% 261
> carry_unhedged full  net   2.8020% 11.0692% 0.253 −32.2491% 1999-06-30 2001-08-31 1.473 −6.5322% 348
> carry_unhedged six   net   3.0254% 10.2899% 0.294 −31.0765% 2017-07-31 2022-08-31 1.483 261
> carry_unhedged full  gross 2.9788% — 0.269 — — — — 348

> Claim: the cost charge is exactly the formula, not a fitted haircut — it reconciles to the turnover to the sixth decimal.
> Number: `cost_t = 2 × 0.5 bp × 1e-4 × turnover_t`. Mean turnover 1.4731 → annual drag `12 × 2 × 0.5e-4 × 1.4731 = 0.0017678`. Gross minus net annualised return is `0.0076749 − 0.0059071 = 0.0017678`. Identical.
> Rows (date, r_gross, cost, r_net, turnover):
> 1997-08-31 −0.007591 0.000500 −0.008091 5.000000  (the first month: a full entry of 2B = 10 years of duration)
> 1997-09-30 −0.002663 0.000042 −0.002704 0.416667
> 1997-10-31  0.000489 0.000125  0.000364 1.250000
> 2022-07-31 −0.009547 0.000571 −0.010119 5.714286
> 2022-10-31 −0.010800 0.000036 −0.010835 0.357143
> 2023-05-31  0.015085 0.000036  0.015049 0.357143

> Claim: the worst month of the sample is a real curve event with named legs, not a data artefact.
> Number: 1998-11-30, `r_net = −2.65%`. The book was long JP across the curve into the JGB sell-off of late 1998; the five largest negative contributions are all JP long legs, and the largest single bucket return is JP 20y at **−13.76%** in the month.
> Rows (rank, date, r_net, country, tenor, leg, wd, r_bucket, contribution):
> 1 loss 1998-11-30 −0.026516 JP 10 long 0.416667 −0.087550 −0.003869
> 1 loss 1998-11-30 −0.026516 JP 7  long 0.416667 −0.058111 −0.003587
> 1 loss 1998-11-30 −0.026516 JP 20 long 0.416667 −0.137560 −0.003432
> 1 loss 1998-11-30 −0.026516 JP 5  long 0.416667 −0.036011 −0.003062
> 1 loss 1998-11-30 −0.026516 JP 3  long 0.416667 −0.015965 −0.002237
> 1 loss 1998-11-30 −0.026516 DE 5  long 0.416667  0.007123  0.000660 (the largest offset)

> Claim: 2022 is a loss and it is spread across the year, not one month.
> Number: `−4.41%` for the twelve months, with a 2022-only drawdown of `−4.69%`. Eight of the twelve months are negative; the two worst are February (`−1.70%`) and August (`−1.70%`), and no single month is more than 40% of the year's loss.
> Rows (date, r_gross, cost, r_net, n_long):
> 2022-02-28 −0.016924 0.000100 −0.017024 10
> 2022-07-31 −0.009547 0.000571 −0.010119 13
> 2022-08-31 −0.016521 0.000464 −0.016986 14
> 2022-10-31 −0.010800 0.000036 −0.010835 14
> 2022-12-31 −0.010942 0.000200 −0.011142 15
> 2022-09-30  0.012763 0.000143  0.012620 14 (the best month of the year)

> Claim: nothing computed before it was logged, and the log has exactly the two rows this step is allowed.
> Number: `reports/specifications.csv` has 2 data rows. Both carry `git_commit = 00fa75e` (the 5.2 commit, which was HEAD when they ran) and the same `config_hash 68b55883b46e`, because neither 5.3 variant overrides anything. `test_run_logs_specification_before_computing` shows a computing function called with an unlogged `run_id` raises.
> Rows (run_id, timestamp_utc, git_commit, config_hash, label, note):
> 20260923T165737-68b55883b46e 2026-09-23T16:57:37+00:00 00fa75e 68b55883b46e carry_hedged (empty)
> 20260923T165738-68b55883b46e 2026-09-23T16:57:38+00:00 00fa75e 68b55883b46e carry_unhedged (empty)
> — and the suffix rule in `speclog.log_run` is why two runs in the same second do not collide
> — `n_closed` is 0 in every month of both runs: 5.1's universe join already excluded the closed buckets
> — `n_long == n_short` in all 348 months of both runs

## Not verified

- That the hedged return is the right hedged return. It is 4.3's
  `r_hedged` column, reviewed there; 5.3 only sums `w × r`. The check that
  would settle it is 4.3's covered-interest-parity identity, already run.
- That 0.5 bp per year of duration is the right cost. Amendment 2's
  `cost0` and `cost2x` variants are what will settle how much the answer
  rests on it, and they are in 5.5, which this session did not reach.
- The deflated Sharpe. It needs `N` = the final `specifications.csv` row
  count, which is not final until 5.5 has run. Nothing here reports it.

## Open

- The unhedged book earns 2.80% a year against the hedged book's 0.59%, on
  five times the vol, for almost the same Sharpe (0.253 against 0.265). On
  the face of it the unhedged run is a currency bet wearing a carry book's
  clothes — its worst drawdown (`−32%`, 1999-06 to 2001-08) is the dollar
  rally, not a curve event. That is exactly the failure mode the brief
  names, and it is why the headline was fixed as hedged before any of this
  was seen. It is not resolved here; 6.1's decomposition is where it gets
  settled.
- The drawdown peak is 2019-12 and the trough 2022-12: three years, and it
  contains both the COVID months where `k` fell to 5 (see `review/5.2.md`)
  and the 2022 rate shock. Whether it is one drawdown or two regimes
  stitched together by a thin book is not something the number can say.

## Reviewer reads (max 6, in order)

1. `data/checks/metrics_carry_hedged.csv` — the headline, both windows, gross and net.
2. `data/checks/extreme_months_carry_hedged.csv` — the 10 best and 10 worst months with their legs; 1998-11 is rank 1 loss.
3. `reports/specifications.csv` — two rows, and nothing computed before them.
4. `src/curvecarry/backtest.py` — the docstring states the three decisions above; `monthly_returns` is the engine.
5. `tests/test_backtest.py` — `test_net_notional_is_financed` and `test_timing_signal_t_return_t_to_t_plus_1`.
6. `data/processed/backtest_carry_hedged.parquet` — the monthly series behind every number above.
